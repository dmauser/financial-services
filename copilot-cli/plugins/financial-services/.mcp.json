{
  "_comment": "Empty by default - Copilot CLI plugin host tries to connect to every server listed here on plugin load. Most upstream connectors require a paid subscription. Open .mcp.json.template (sibling file), copy the block(s) you have credentials for into this file's `mcpServers`, supply env vars per the provider docs, and restart Copilot CLI.",
  "mcpServers": {}
}
